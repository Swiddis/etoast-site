worker_processes 4;

events {
    worker_connections 4096;
}

http {
    include /etc/nginx/mime.types;

    server {
        server_name localhost;
        listen 8080 proxy_protocol;
        charset utf-8;

        location / {
            root /www/data;
            index index.html;
        }
    }
}
